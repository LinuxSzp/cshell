#编译
    1. 进入上级目录.., 先编译生成库
        $ make
    2. 在demo目录中,编译生成可执行文件cshell_demo
        $ make

#运行
    1.  $ ./cshell_demo

    2. 再另一个终端上运行
        $ telnet 127.0.0.1 1234

    3. 输入注册的命令
        1).start、stop、watch、clear, 输入其他命令则显示已注册的命令;
        2).输入已注册的命令, 执行相应的操作.
